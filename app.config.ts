export default defineAppConfig({
    ui: {
        input: {
            color: {
                light: {
                    "filled": 'shadow-sm bg-white text-gray-900 ring-1 ring-inset ring-gray-300 focus:ring-2 focus:ring-base-500'
                },
            },
            icon: {
                base: 'flex-shrink-0 text-base-400 dark:text-base-500'
            }

        }
    }
})
